### hi, i guess

never really had a github account before. never needed one — most of my work lived in private repos on a friend's server or just local branches nobody ever saw.

i've been writing C# for a few years now, mostly game server stuff. packet parsing, session state, server-side logic — the boring parts that make everything else work. got into it through NosTale private servers, ended up going way deeper than i planned.

now i'm part of [@PhantomMasterTeam](https://github.com/PhantomMasterTeam) and we're building something proper for once. figured it's time to actually have a profile somewhere.

---

```text
current stack: .NET 8 | PostgreSQL | Redis | Docker | K8s
```